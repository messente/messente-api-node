/**
 * Messente API
 * [Messente](https://messente.com) is a global provider of messaging and user verification services.  * Send and receive SMS, Viber, WhatsApp and Telegram messages. * Manage contacts and groups. * Fetch detailed info about phone numbers. * Blacklist phone numbers to make sure you're not sending any unwanted messages.  Messente builds [tools](https://messente.com/documentation) to help organizations connect their services to people anywhere in the world.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: messente@messente.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SyncNumberLookupResult from './SyncNumberLookupResult';

/**
 * The SyncNumberLookupSuccess model module.
 * @module model/SyncNumberLookupSuccess
 * @version 2.1.0
 */
class SyncNumberLookupSuccess {
    /**
     * Constructs a new <code>SyncNumberLookupSuccess</code>.
     * A container for number lookup response
     * @alias module:model/SyncNumberLookupSuccess
     * @param request_id {String} ID of the request
     * @param result {Array.<module:model/SyncNumberLookupResult>} A container for phone number info objects
     */
    constructor(request_id, result) { 
        
        SyncNumberLookupSuccess.initialize(this, request_id, result);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, request_id, result) { 
        obj['request_id'] = request_id;
        obj['result'] = result;
    }

    /**
     * Constructs a <code>SyncNumberLookupSuccess</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SyncNumberLookupSuccess} obj Optional instance to populate.
     * @return {module:model/SyncNumberLookupSuccess} The populated <code>SyncNumberLookupSuccess</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SyncNumberLookupSuccess();

            if (data.hasOwnProperty('request_id')) {
                obj['request_id'] = ApiClient.convertToType(data['request_id'], 'String');
            }
            if (data.hasOwnProperty('result')) {
                obj['result'] = ApiClient.convertToType(data['result'], [SyncNumberLookupResult]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SyncNumberLookupSuccess</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SyncNumberLookupSuccess</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SyncNumberLookupSuccess.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['request_id'] && !(typeof data['request_id'] === 'string' || data['request_id'] instanceof String)) {
            throw new Error("Expected the field `request_id` to be a primitive type in the JSON string but got " + data['request_id']);
        }
        if (data['result']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['result'])) {
                throw new Error("Expected the field `result` to be an array in the JSON data but got " + data['result']);
            }
            // validate the optional field `result` (array)
            for (const item of data['result']) {
                SyncNumberLookupResult.validateJSON(item);
            };
        }

        return true;
    }


}

SyncNumberLookupSuccess.RequiredProperties = ["request_id", "result"];

/**
 * ID of the request
 * @member {String} request_id
 */
SyncNumberLookupSuccess.prototype['request_id'] = undefined;

/**
 * A container for phone number info objects
 * @member {Array.<module:model/SyncNumberLookupResult>} result
 */
SyncNumberLookupSuccess.prototype['result'] = undefined;






export default SyncNumberLookupSuccess;

