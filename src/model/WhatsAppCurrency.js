/**
 * Messente API
 * [Messente](https://messente.com) is a global provider of messaging and user verification services.  * Send and receive SMS, Viber, WhatsApp and Telegram messages. * Manage contacts and groups. * Fetch detailed info about phone numbers. * Blacklist phone numbers to make sure you're not sending any unwanted messages.  Messente builds [tools](https://messente.com/documentation) to help organizations connect their services to people anywhere in the world.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: messente@messente.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WhatsAppCurrency model module.
 * @module model/WhatsAppCurrency
 * @version 2.2.0
 */
class WhatsAppCurrency {
    /**
     * Constructs a new <code>WhatsAppCurrency</code>.
     * Whatsapp currency object.
     * @alias module:model/WhatsAppCurrency
     * @param fallback_value {String} Default text if localization fails.
     * @param code {String} Currency code as defined in ISO 4217.
     * @param amount_1000 {String} Amount multiplied by 1000.
     */
    constructor(fallback_value, code, amount_1000) { 
        
        WhatsAppCurrency.initialize(this, fallback_value, code, amount_1000);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fallback_value, code, amount_1000) { 
        obj['fallback_value'] = fallback_value;
        obj['code'] = code;
        obj['amount_1000'] = amount_1000;
    }

    /**
     * Constructs a <code>WhatsAppCurrency</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WhatsAppCurrency} obj Optional instance to populate.
     * @return {module:model/WhatsAppCurrency} The populated <code>WhatsAppCurrency</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WhatsAppCurrency();

            if (data.hasOwnProperty('fallback_value')) {
                obj['fallback_value'] = ApiClient.convertToType(data['fallback_value'], 'String');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('amount_1000')) {
                obj['amount_1000'] = ApiClient.convertToType(data['amount_1000'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WhatsAppCurrency</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WhatsAppCurrency</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WhatsAppCurrency.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['fallback_value'] && !(typeof data['fallback_value'] === 'string' || data['fallback_value'] instanceof String)) {
            throw new Error("Expected the field `fallback_value` to be a primitive type in the JSON string but got " + data['fallback_value']);
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['amount_1000'] && !(typeof data['amount_1000'] === 'string' || data['amount_1000'] instanceof String)) {
            throw new Error("Expected the field `amount_1000` to be a primitive type in the JSON string but got " + data['amount_1000']);
        }

        return true;
    }


}

WhatsAppCurrency.RequiredProperties = ["fallback_value", "code", "amount_1000"];

/**
 * Default text if localization fails.
 * @member {String} fallback_value
 */
WhatsAppCurrency.prototype['fallback_value'] = undefined;

/**
 * Currency code as defined in ISO 4217.
 * @member {String} code
 */
WhatsAppCurrency.prototype['code'] = undefined;

/**
 * Amount multiplied by 1000.
 * @member {String} amount_1000
 */
WhatsAppCurrency.prototype['amount_1000'] = undefined;






export default WhatsAppCurrency;

