/**
 * Messente API
 * [Messente](https://messente.com) is a global provider of messaging and user verification services.  * Send and receive SMS, Viber, WhatsApp and Telegram messages. * Manage contacts and groups. * Fetch detailed info about phone numbers. * Blacklist phone numbers to make sure you're not sending any unwanted messages.  Messente builds [tools](https://messente.com/documentation) to help organizations connect their services to people anywhere in the world.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: messente@messente.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The StatisticsReport model module.
 * @module model/StatisticsReport
 * @version 2.0.0
 */
class StatisticsReport {
    /**
     * Constructs a new <code>StatisticsReport</code>.
     * Report for one country
     * @alias module:model/StatisticsReport
     * @param totalMessages {Number} Sum of all messages
     * @param totalPrice {String} Price for all messages
     * @param country {String} Target country of all messages
     */
    constructor(totalMessages, totalPrice, country) { 
        
        StatisticsReport.initialize(this, totalMessages, totalPrice, country);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, totalMessages, totalPrice, country) { 
        obj['total_messages'] = totalMessages;
        obj['total_price'] = totalPrice;
        obj['country'] = country;
    }

    /**
     * Constructs a <code>StatisticsReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StatisticsReport} obj Optional instance to populate.
     * @return {module:model/StatisticsReport} The populated <code>StatisticsReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StatisticsReport();

            if (data.hasOwnProperty('total_messages')) {
                obj['total_messages'] = ApiClient.convertToType(data['total_messages'], 'Number');
            }
            if (data.hasOwnProperty('total_price')) {
                obj['total_price'] = ApiClient.convertToType(data['total_price'], 'String');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StatisticsReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StatisticsReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StatisticsReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['total_price'] && !(typeof data['total_price'] === 'string' || data['total_price'] instanceof String)) {
            throw new Error("Expected the field `total_price` to be a primitive type in the JSON string but got " + data['total_price']);
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }

        return true;
    }


}

StatisticsReport.RequiredProperties = ["total_messages", "total_price", "country"];

/**
 * Sum of all messages
 * @member {Number} total_messages
 */
StatisticsReport.prototype['total_messages'] = undefined;

/**
 * Price for all messages
 * @member {String} total_price
 */
StatisticsReport.prototype['total_price'] = undefined;

/**
 * Target country of all messages
 * @member {String} country
 */
StatisticsReport.prototype['country'] = undefined;






export default StatisticsReport;

